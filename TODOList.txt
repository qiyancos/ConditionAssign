1. 完成剩余的cpp文件
2. 优化任务1，优化MifLayer和MifItem，区分只读类型和写类型，避免额外的锁操作(已完成)
3. 优化任务2, 优化Operator，所有的Operator均实例化，公有成员则处理成静态成员
4. 代码风格，所有的指针类型变量一律在结尾加上后缀Ptr
5. 优化任务3，对于地理位置关系判断，All系列的关系可以转换成某个One取非，尽量进行转换(已取消)
6. 功能增强，添加对地理结构接触相关的符号逻辑(已完成)

存在潜在问题：
1. 在lock与unlock之间会存在错误状态的return，如果错误return无法导致程序结束就会导致死锁
