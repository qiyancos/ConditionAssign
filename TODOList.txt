1. 完成剩余的cpp文件
2. 优化任务1，优化MifLayer和MifItem，区分只读类型和写类型，避免额外的锁操作(已完成)
3. 优化任务2, 优化Operator，所有的Operator均实例化，公有成员则处理成静态成员(已完成)
4. 代码风格，所有的指针类型变量一律在结尾加上后缀Ptr
5. 优化任务3，对于地理位置关系判断，All系列的关系可以转换成某个One取非，尽量进行转换(已取消)
6. 功能增强，添加对地理结构接触相关的符号逻辑(已完成)
7. 所有使用new的地方都需要进行检查，确保最后会析构
8. 重构ExecutorJob，支持更好的扩展性，这一部分扩展的需求较小(已取消)
9. 检查所有类的构造和析构函数是否正确
10. 死锁的检查(尽量避免wait并优化干扰，避免lock影响速度)
11. 添加输出层不同时的assign方法，需要新建MifItem并拷贝(已完成)
12. 添加输出层为空时的创建动作(已完成)
13. 如果输入层和输出层一致时的layer的打开动作设计，是否可以打开两次，一个只读，一个写操作
14. 由于并行性要求，新的configItem之间顺序不再保证
15. 将一部分简单共享数据处理成原子变量
16. 使用lock_guard替换现有的所有锁操作（部分完成）
17. 优化任务，简化备选队列为单队列(已完成)
18. 检查是否存在可能的工作项有依赖但是没有加wait操作
19. 目前的逻辑，如果输入层和输出层不同，且输出层已存在，那么赋值操作会在和满足条件的元素索引相同的目标层元素生效
20. 调整debug宏，支持按格式输出(已完成)
21. 排查执行路线上冗余的wait操作，避免反复wait同一个变量
22. 深度优化，减少逻辑表达式中不必要的表达式，比如A&&B，A不满足则A&&B对应节点直接设置为不满足，而不计算B(已完成)

存在潜在问题：
1. 在lock与unlock之间会存在错误状态的return，如果错误return无法导致程序结束就会导致死锁(可以使用lock_guard解决)
